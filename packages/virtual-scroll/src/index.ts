/**
 * Public package entrypoint.
 *
 * Consumers typically call `defineVirtualScrollElement()` once during startup and then
 * use the registered `<virtual-scroll>` custom element in markup or framework renders.
 */
export { VirtualScrollElement, defineVirtualScrollElement } from "./element.js";
